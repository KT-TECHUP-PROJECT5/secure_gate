# Cursor AI 컨텍스트 파일
.cursor/

# 보안 검사 결과 파일 (CI에서 생성, 로컬 보관 불필요)
security/reports/*.json
security/reports/*.jsonl
security/reports/*.sarif
security/reports/*.txt
security/reports/*.md

# 로컬에서 내려받은 Actions Artifact 샘플
testsFiles/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# 취약 웹 로컬 실행 파일
web/.env
web/app/uploads/*
!web/app/uploads/.gitkeep

# OS
.DS_Store
Thumbs.db
