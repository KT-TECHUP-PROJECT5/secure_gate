name: CD - Staging Deploy

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  # ──────────────────────────────────────────
  # 1. Docker Build - 4차 구현에서 실제 연결 예정
  # ──────────────────────────────────────────
  docker-build:
    name: Docker Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: "[Placeholder] Docker Build"
        run: |
          echo "Docker Build placeholder"
          echo "Image tag: staging-${{ github.sha }}"
          echo "Image tag: latest"
          echo "Dockerfile 위치 및 빌드 명령어는 4차 구현에서 확정 예정"

  # ──────────────────────────────────────────
  # 2. Staging Deploy - 4차 구현에서 실제 연결 예정
  # ──────────────────────────────────────────
  staging-deploy:
    name: Staging Deploy
    runs-on: ubuntu-latest
    needs: docker-build
    steps:
      - uses: actions/checkout@v4

      - name: "[Placeholder] Staging Deploy"
        run: |
          echo "Staging Deploy placeholder"
          echo "배포 방식 (EC2 / Docker Compose / Cloud) 확정 후 실제 배포 명령어로 교체 예정"

  # ──────────────────────────────────────────
  # 3. Post-deploy Validation - D파트 연결 예정
  # ──────────────────────────────────────────
  post-deploy-validation:
    name: Post-deploy Validation
    runs-on: ubuntu-latest
    needs: staging-deploy
    steps:
      - uses: actions/checkout@v4

      - name: "[Placeholder] Health Check"
        run: |
          echo "Health Check placeholder"
          echo "D파트의 Health Check Endpoint 확정 후 교체 예정"

      - name: "[Placeholder] Smoke Test"
        run: |
          echo "Smoke Test placeholder"
          echo "D파트의 Smoke Test 명령어 확정 후 교체 예정"

      - name: "[Placeholder] Post-deploy DAST"
        run: |
          echo "Post-deploy DAST placeholder"
          echo "D파트의 ZAP 명령어 및 Staging URL 확정 후 교체 예정"

      - name: "[Placeholder] Security Header Check"
        run: |
          echo "Security Header Check placeholder"
          echo "D파트의 보안 헤더 검증 기준 확정 후 교체 예정"
